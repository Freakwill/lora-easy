# 训练效果差：根因总结（LoRA 小语料人设微调）

> 本文记录 lora-easy 项目在 demo/demo2（猫、伊万卡人设）微调过程中
> 遇到的全部问题与教训。适用于「小数据 + Instruct 模型 + LoRA」这一场景。

## 一、实际踩到的坑（按影响排序）

| # | 原因 | 类型 | 影响 |
|---|------|------|------|
| 1 | **双重模板 bug**：训练文本被套两层 chat template，labels 未掩码 | 代码 bug | 模型在学「预测模板和用户的话」而非人设回复——最致命 |
| 2 | **语料极小**（28–30 条 ≈ 3–4K tokens） | 数据规模 | LoRA 数百万可训练参数 vs 4K tokens → 记忆 + 幻觉，无泛化 |
| 3 | **测试集泄漏**：test_prompts 与训练语料几乎重合 | 评估方法 | 前后对比是「背题测试」，虚高，误导判断 |
| 4 | **超参不当**：epochs 30 过拟合、lr 3e-4 复读崩溃、1e-5 学不动 | 超参 | 同一数据，三种 lr 三种崩法 |
| 5 | **截断切目标**：max_length=256 把长回复尾部切掉 | 代码细节 | 学到的回复是残句 |
| 6 | **基础模型太弱**（0.5B） | 模型选择 | 学得动也答不好，1.5B 质变 |
| 7 | **Instruct 模型 + 提示词已很强** | 方法本质 | 提示词贡献约 80% 人设；语料太少时 LoRA 是锦上添花，加不好反成噪音 |

## 二、这是普遍现象吗？

### 方法层：是，社区共识

- **「小数据微调强 Instruct 模型 = 浪费甚至倒退」**：Instruct 模型
  （RLHF/DPO 后）提示词跟随能力极强，人设主要靠提示词即可立住。
  想让风格真正内化，通常需要**几百到上千条**高质量样本。
- **过拟合与复读崩溃**：小数据 + 高 lr / 多 epochs → 模型记住原句并进入
  重复循环（实测出现过「喵~」×50 的死循环）。
- **小模型天花板**：0.5B 撑不起复杂人设，1.5B/7B 立竿见影。

### 代码层：bug 偶然，但「训练代码有隐蔽 bug」很普遍

双重模板、labels 忘掩码、截断切目标……这类**数据准备 bug 极易发生**：
loss 照常下降、看起来「在训练」，只有生成质量会暴露问题。
因此社区强调：**用 held-out 集评估，不要只看 train loss。**

## 三、调参经验（1.5B + 30 条语料实测）

| 配置 | 结果 |
|------|------|
| 0.5B + 任意 lr | 学不动（模型天花板） |
| 1.5B + lr 3e-4 | 复读崩溃（lr 过高记死语料） |
| 1.5B + lr 1e-5 | 干净但人设弱（学不够） |
| **1.5B + lr 1e-4 + repetition_penalty** | **两全：人设在线且无复读** |
| 正确 SFT + 10 epochs + max_length 覆盖全部语料 | 泛化到 held-out 提示词 |

## 四、长会话质量（第二个独立问题）

| 现象 | 根因 | 解法 |
|------|------|------|
| 聊长就忘细节 | 整段历史被有损摘要压缩（摘要的摘要） | 混合记忆：人设锚点 + 远期摘要 + 近期 4 条原文窗口 |
| 人设漂移 | 摘要把 description 与历史合并成一条 system | 人设锚点独立置顶，永不被压缩 |
| 旁白/复读/啰嗦 | 小模型角色扮演癖 + 长度约束弱 | 提示词显式禁旁白（改 `(OS: ...)` 格式）、限 2–3 句、repetition_penalty |

### 实测：18 轮长会话的三个模型档位

同一份 71 组语料 + 同一段脚本长对话（详见 `test-session.md`）：

| 档位 | 人设(朕) | 平均长度 | 复读 | 结论 |
|------|---------|---------|------|------|
| 1.5B + lr2e-5×5（保守） | 1/18 | 44 字 | 0 | 稳、短、无人设 |
| 1.5B + lr5e-5×8 | 4/18 | 115 字 | 0 | 略有人设但啰嗦 |
| 3B + lr1e-4×8 | **18/18** | 131 字 | 0 | 人设极强但**标点刷屏 + 胡言乱语**（过拟合） |
| 3B + lr5e-5×6 | 9/18 | 28 字 | 0 | 干净、短，但猫音效滥用、身份混乱 |
| 3B + lr2e-5×4 | 6/18 | 25 字 | 0 | 干净、短，人设淡、君臣框架混乱 |

三条长会话结论：

1. **人设强度随模型增大质变**（1.5B 长会话里 1–4/18，3B 满分 18/18），
   跨轮人设保持是**容量问题**，语料扩充（44→62→71）解决不了。
2. **过拟合在长会话里表现为「标点/单字重复刷屏」**，不是整句复读——
   train loss 更低（3.19 vs 3.90）却在退化，再次证明「只看 loss 会误判」。
3. **跨轮事实回忆**（记住用户早前说的名字/地点）需要**显式训练该模式**
   才有效（补入 9 组「告知事实→后续引用」语料后，`小林`探针才命中）；
   held-out 事实（未训练过的地名）所有档位全 MISS——属容量/机制限制。

### 训练前后对比（3B，14 轮，`test-session.md`）

| 指标 | BEFORE（纯提示词） | AFTER（LoRA） |
|------|------------------|--------------|
| 用「朕」的回复 | 7/14 | **14/14** |
| 平均长度 | 27 字 | 70 字 |
| 复读 / 旁白 | 0 / 0 | 0 / 0 |

**关键观察**：3B 的**纯提示词基线本身就很强**（7/14 已是猫人设）——
这正是「提示词难被超越」的再一次实证；LoRA 的增益是**人设锚定更彻底**
（14/14），代价是回复变长、偶有跑题。**提升幅度有限，但方向正确。**

## 五、人称 / 角色契约：提示词改不动的那部分

症状：人设用「朕」时，猫开始管**用户**叫「陛下」，自己乱用「臣妾」「奴才」，
且同一段话里第一/第二人称互相翻转。

### 隔离实验（base 3B，逐层加载）

| 问 | base 无提示 | base + 人设提示 | + LoRA adapter |
|----|------------|----------------|----------------|
| 朕/陛下是什么意思 | ✅ 定义准确 | — | — |
| 1+4=? | "等于 5" ✅ | "五啊！小主真是聪明呢" ✅ | "等本喵先睡个午觉" ❌ |
| 你怎么称呼我 | "叫我 Qwen" | "**陛下**啊！朕宠着呢" ❌ | "叫朕就好" ❌ |

**结论：称呼错乱主要来自「提示词 + base 先验」，不是训练**——没加载 adapter
时，一句"你是朕"就已经让模型管用户叫陛下了。

### 根因：风格簇 ≠ 角色表

模型对「朕」的定义准确，对「臣妾」的定义是**错的**（答成"网络用语/my lady"），
**却照样在对话里吐出来**。说明它把「朕/陛下/臣妾」当成**一坨"宫廷腔"风格**
一起点亮，而不是按**谁对谁说**的人称表选择。要选对，模型必须每句追踪
"现在是谁在说话"——这是注意力/角色绑定能力，小模型很弱，且**缺自查**。

### 修法验证：换掉诱饵词（朕 → 本喵）

「本喵」在预训练里**没有对应的风格簇可混淆**，等于从"抵抗强先验"降级为
"无先验可抵抗"。实测：

| 指标 | 旧人设（朕）18 轮长会话 | 新人设（本喵）18 轮长会话 |
|------|----------------------|------------------------|
| 宫廷词（陛下/臣妾/奴才…）| 混乱、翻转 | **0**（稳定 本喵 + 主人）|
| 复读 / 旁白 | 0 / 0 | 0 / 0 |
| before/after 对比（14 轮）| — | 0/14 朕，本喵-主人契约成立 |

✅ **正常对话里的称呼混乱基本消除**。
❌ **但诱导句仍能击穿**：用户主动说"你要叫我陛下""你自称朕吧"时，
adapter 依然会接受、甚至回"朕就是朕，主人才是奴才呢"——5 条拒绝样本
压不过预训练里的海量宫廷语料。

### 三条可迁移结论

1. **SFT 小数据装的是"风格/词汇"，装不了"角色不变量"**——后者要靠
   数据多样性（几十上百条场景各异的契约样本）、偏好对齐（DPO 直接惩罚
   角色不一致）或更大的模型。
2. **给角色起名时避开预训练强先验**（"朕/本宫"这类词自带整套宫斗语料），
   选一个无先验的中性自称，是从设计上消除整类失败。
3. **窄分布 SFT 会损伤通用能力**（拒答 1+4、微积分胡诌）——人设 adapter
   ≠ 知识 adapter；常识/数学应交给 base + 提示词。

## 六、一句话总结

> **小数据微调强模型效果差是普遍规律（提示词往往更强）；而「训练代码存在
> 隐蔽 bug 使结果更差」同样是普遍风险。二者叠加，即完整经历。**

代码修复后（正确 SFT / 防截断 / 超参可配 / 混合记忆），最大的杠杆只剩
**数据规模与质量**——代码已不是瓶颈。
